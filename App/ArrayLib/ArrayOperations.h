#pragma once
#include <iostream>
#include "Array.h"

class ArrayOperations
{
private:
    /* data */
    Array *array;
public:
    ArrayOperations(/* args */);
    ~ArrayOperations();
};

ArrayOperations::ArrayOperations(/* args */)
{
}

ArrayOperations::~ArrayOperations()
{
}
